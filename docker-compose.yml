version: "3.9"
services:
  backend:
    build: ./backend
    volumes:
      - ./data:/data
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend


